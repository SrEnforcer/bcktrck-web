# TSF++ Audit — api and frontend

**Target:** api/, frontend/
**Focus:** types,boundary,annotations
**Standard:** @tsfpp/standard v1.0.0
**Date:** 2026-05-16 23:29
**Status:** ✅ Resolved

---

## Summary

All MUST and SHOULD violations listed in this report were fixed slice-by-slice. Boundary behavior remains compliant and verification checks are green (`pnpm typecheck`, `pnpm lint`, `pnpm test`).

| Category    | Violations | Deviations | Passed |
|-------------|-----------|------------|--------|
| Types       | 0         | 0          | 10     |
| Purity      | 0         | 4          | 10     |
| Boundary    | 0         | 0          | 10     |
| Annotations | 0         | 0          | 10     |
| Complexity  | 0         | 0          | 10     |

Resolved highlights:
1. Added JSDoc coverage for all exported symbols touched by the audit target.
2. Removed mutable UI type shapes flagged by the report (`ReadonlyArray`/`readonly` props).
3. Replaced selected ad-hoc unknown-value helpers with prelude-based field decoders.

---

## Slices

| # | Path | Status |
|---|------|--------|
| 1 | `api/src/routes/*.ts` | ✅ |
| 2 | `api/src/server.ts` | ✅ |
| 3 | `api/src/types/bcktrck-engine.d.ts` | ✅ |
| 4 | `frontend/src/hooks/useCompiledSvg.ts` | ✅ |
| 5 | `frontend/src/hooks/useSubtreeIsolation.ts` | ✅ |
| 6 | `frontend/src/App.tsx` | ✅ |
| 7 | `frontend/src/components/*.tsx` | ✅ |
| 8 | `frontend/src/hooks/*.ts` (other hooks) | ✅ |
| 9 | `frontend/src/lib/*.ts` + `frontend/src/logging/logger.ts` | ✅ |
| 10 | `frontend/src/main.tsx`, `frontend/src/globals.d.ts`, `frontend/vite.config.ts`, `frontend/vitest.config.ts` | ✅ |

---

<!-- Slices are appended below as the audit progresses -->

> Note: findings listed per slice are the original audit findings and are retained for traceability; each slice status now reflects post-fix verification.

### Slice 1 — `api/src/routes/*.ts`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| CLEAN | `api/src/routes/compile.ts` | CLEAN | Boundary flow is compliant: `extractContext` → schema validation → use-case call → `apiErrorToResponse`/`okResponse`. |
| CLEAN | `api/src/routes/subtrees.ts` | CLEAN | Boundary flow is compliant and no forbidden TypeScript constructs were found. |
| CLEAN | `api/src/routes/stylePack.ts` | CLEAN | Boundary mapping and annotations are present; exported handler includes JSDoc with `@param` and `@returns`. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 4 — `frontend/src/hooks/useCompiledSvg.ts`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `line 160` | MUST | Exported type `UseCompiledSvgInput` has no JSDoc block. |
| 7.x | `line 168` | MUST | Exported type `UseCompiledSvgResult` has no JSDoc block. |
| 8.x | `line 40` | SHOULD | Local `asNumber`/`asString` decoding helpers can be replaced with prelude unknown decoders (`getNumberField`/`getStringField`) to reduce custom parsing glue. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 5 — `frontend/src/hooks/useSubtreeIsolation.ts`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `line 11` | MUST | Exported type `SubtreeIsolationMode` has no JSDoc block. |
| 7.x | `line 13` | MUST | Exported type `UseSubtreeIsolationInput` has no JSDoc block. |
| 7.x | `line 19` | MUST | Exported type `UseSubtreeIsolationResult` has no JSDoc block. |
| 8.x | `line 31` | SHOULD | Local `isSubtreeEntry`/`decodeEntries` can be reduced using prelude field decoders (`getTypedField`, `fromUnknownArrayOf`) for consistent unknown parsing. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 6 — `frontend/src/App.tsx`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 2.x | `line 255` | MUST | State uses mutable array type `string[]` (`undoStack`) instead of `ReadonlyArray<string>`. |
| 7.x | `line 580` | MUST | Exported default component `App` has no JSDoc block. |
| 8.x | `line 81` | SHOULD | `isPrintPageFormat`/`isThemePreference`/`isStylePackChoice` are local validation helpers that could be consolidated with prelude-style decoders or a shared smart-constructor module. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 7 — `frontend/src/components/*.tsx`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `frontend/src/components/AppSections.tsx:28` | MUST | Exported component `WorkspaceTopbar` has no JSDoc block. |
| 7.x | `frontend/src/components/AppSections.tsx:127` | MUST | Exported component `EditorPanel` has no JSDoc block. |
| 7.x | `frontend/src/components/BcktrckEditor.tsx:13` | MUST | Exported component `BcktrckEditor` has no JSDoc block. |
| 2.x | `frontend/src/components/BcktrckEditor.tsx:5` | MUST | `BcktrckEditorProps` fields are not marked `readonly`. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 8 — `frontend/src/hooks/*.ts` (other hooks)

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `frontend/src/hooks/useOverlayViewport.ts:25` | MUST | Exported hook `useOverlayViewport` has no JSDoc block. |
| 7.x | `frontend/src/hooks/usePreviewViewport.ts:36` | MUST | Exported hook `usePreviewViewport` has no JSDoc block. |
| 8.x | `frontend/src/hooks/usePreviewViewport.ts:46` | SHOULD | Session-storage parsing uses ad-hoc `JSON.parse` + shape checks; prelude record/field decoders would reduce custom guard logic. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 9 — `frontend/src/lib/*.ts` + `frontend/src/logging/logger.ts`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `frontend/src/lib/viewportMath.ts:1` | MUST | Exported constants/functions (e.g. `minViewScale`, `clampScale`) lack JSDoc blocks. |
| 7.x | `frontend/src/lib/subtreeSelection.ts:3` | MUST | Exported type/functions in subtree-selection module lack JSDoc blocks. |
| 7.x | `frontend/src/logging/logger.ts:1` | MUST | Exported function `debugLog` has no JSDoc block with `@param`/`@returns`. |
| 8.x | `frontend/src/lib/subtreeSelection.ts:8` | SHOULD | `uniqueInOrder` and ID sanitization logic could be replaced with prelude collection combinators for consistency. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 10 — `frontend/src/main.tsx`, `frontend/src/globals.d.ts`, `frontend/vite.config.ts`, `frontend/vitest.config.ts`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `frontend/src/globals.d.ts:3` | MUST | Exported ambient module declarations (`ParseErr`, `compile`, etc.) have no JSDoc blocks. |
| 6.x | `frontend/src/main.tsx:8` | SHOULD | `throw new Error(...)` is used in app bootstrap; consider boundary-safe fallback rendering pattern to avoid uncaught throw path. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |

### Slice 2 — `api/src/server.ts`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| NOTE | `line 38` | NOTE | `new URL(...)` usage is adapter-only and correctly documented with `DEVIATION(1.9)` + lint suppression. |
| NOTE | `line 48` | NOTE | `new Request(...)` usage is adapter-only and correctly documented with `DEVIATION(1.9)` + lint suppression. |
| CLEAN | `line 63` | CLEAN | Route dispatch delegates boundary logic to dedicated handlers; no raw `new Response(...)` in handlers. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(1.9) | `38` | Node adapter URL construction for Fetch request bridge. |
| DEVIATION(1.9) | `48` | Node adapter Request construction for bridge. |
| DEVIATION(1.9) | `59` | Adapter route extraction from request URL. |
| DEVIATION(1.9) | `66` | Adapter route dispatch parsing. |

### Slice 3 — `api/src/types/bcktrck-engine.d.ts`

**Status:** ✅ Resolved

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 7.x | `line 2` | MUST | Exported type `ParseErr` has no JSDoc block (`@param`/`@returns` not applicable, but export docs are required by project rules). |
| 7.x | `line 49` | MUST | Exported declaration `defaultRenderConfig` has no JSDoc block. |
| 7.x | `line 50` | MUST | Exported function type `compile` has no JSDoc with `@param` and `@returns`. |

#### Checklist

- [x] 1.4 — No bare `interface` (or DEVIATION documented)
- [x] 1.5 — No `any`
- [x] 1.6 — No `!` assertions
- [x] 2.x — `readonly` fields and `ReadonlyArray`
- [x] 3.x — `const` bindings only
- [x] 4.1 — Exhaustive `switch` with `absurd`
- [x] 4.5 — No truthiness checks on non-booleans
- [x] 5.1 — Pipelines via `pipe` from prelude
- [x] 6.x — No `throw` in core
- [x] 7.x — JSDoc on all exports
- [x] 8.x — Prefer prelude ADTs/constructors/helpers (no downstream reimplementation in domain code)
- [x] 9.x — Dependency hygiene (no deprecated dependencies, no banned imports per policy, no layer-violating imports)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| — | — | No deviations in this slice. |
