# TSF++ Audit — api

**Target:** api
**Focus:** all
**Standard:** @tsfpp/standard v1.2.0
**Date:** 2026-05-18 12:23
**Status:** ✅ Resolved

---

## Summary

All previously reported MUST and SHOULD violations were fixed in code and re-verified.

| Category    | Violations | Deviations | Passed | N/A |
|-------------|-----------|------------|--------|-----|
| Types       | 0         | 8          | 11     | 0   |
| Purity      | 0         | 1          | 11     | 0   |
| Boundary    | 0         | 3          | 11     | 0   |
| Annotations | 0         | 0          | 11     | 0   |
| Complexity  | 0         | 0          | 11     | 0   |
| Prelude     | 0         | 0          | 11     | 0   |
| React       | 0         | 0          | 0      | 11  |
| Data        | 0         | 0          | 0      | 11  |
| Security    | 0         | 0          | 11     | 0   |
| Tests       | 0         | 3          | 4      | 7   |

_N/A — focus not applicable to this target (e.g. React row when no `.tsx` files in scope)_

### Top priority issues

1. Resolved: module-level `@packageDocumentation` added to all previously flagged files.
2. Resolved: missing exported-symbol JSDoc added in shim and config modules.
3. Resolved: core immutability/nullability violations fixed in handler and server adapters.

---

## Slices

| # | Path | Status |
|---|------|--------|
| 1 | `api/src/routes/compile.test.ts` | ✅ |
| 2 | `api/src/routes/compile.ts` | ✅ |
| 3 | `api/src/routes/stylePack.test.ts` | ✅ |
| 4 | `api/src/routes/stylePack.ts` | ✅ |
| 5 | `api/src/routes/subtrees.test.ts` | ✅ |
| 6 | `api/src/routes/subtrees.ts` | ✅ |
| 7 | `api/src/server.test.ts` | ✅ |
| 8 | `api/src/server.ts` | ✅ |
| 9 | `api/src/test-support/engineShim.ts` | ✅ |
| 10 | `api/src/types/bcktrck-engine.d.ts` | ✅ |
| 11 | `api/vitest.config.ts` | ✅ |

---

<!-- Slices are appended below as the audit progresses -->

### Slice 1 — `api/src/routes/compile.test.ts`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 1.9  | `line 16` | NOTE | `new Request(...)` is covered by `DEVIATION(1.9)` at lines 14-15 for handler-boundary testing. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref            | Line   | Justification |
|----------------|--------|---------------|
| DEVIATION(1.9) | `14-15` | Fetch `Request` instantiation is required in adapter-boundary handler tests. |

### Slice 2 — `api/src/routes/compile.ts`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| SS1  | `line 1` | MUST | File exports a public symbol but has no module-level JSDoc with `@packageDocumentation`. |
| 2.5  | `line 21` | SHOULD | Config table `previewConfig` is not narrowed with `as const`. |
| 3.x  | `line 21` | MUST | Record fields in `previewConfig` are not declared as `readonly` via an explicit readonly type. |
| 3.x  | `line 72` | MUST | `compileOptions` object shape is mutable and not typed with readonly fields. |
| 8.2  | `line 36` | NOTE | `DEVIATION(8.2)` documents compatibility adapter for `fromZodError` with Zod v4 `issues`. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref            | Line   | Justification |
|----------------|--------|---------------|
| DEVIATION(8.2) | `36`   | Compatibility bridge between boundary Zod error shape and Zod v4 issue shape. |

### Slice 3 — `api/src/routes/stylePack.test.ts`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 1.9  | `line 15` | NOTE | `new Request(...)` is covered by `DEVIATION(1.9)` at lines 13-14 for handler-boundary testing. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref            | Line   | Justification |
|----------------|--------|---------------|
| DEVIATION(1.9) | `13-14` | Fetch `Request` instantiation is required in adapter-boundary handler tests. |

### Slice 6 — `api/src/routes/subtrees.ts`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| SS1  | `line 1` | MUST | File exports a public symbol but has no module-level JSDoc with `@packageDocumentation`. |
| 3.x  | `line 56` | MUST | `subtreeOptions` object is not declared through a readonly record type. |
| 6.3  | `line 58` | MUST | `Option` is collapsed back to `null` (`styleSource: ... ? ... : null`) instead of preserving `Option` in-domain. |
| 8.2  | `line 19` | NOTE | `DEVIATION(8.2)` documents compatibility adapter for `fromZodError` with Zod v4 `issues`. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref            | Line   | Justification |
|----------------|--------|---------------|
| DEVIATION(8.2) | `19`   | Compatibility bridge between boundary Zod error shape and Zod v4 issue shape. |

### Slice 7 — `api/src/server.test.ts`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| CLEAN | `line 1` | CLEAN | No TSF++ rule violations detected in this slice under active focus. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| —   | —    | No deviations declared in this slice. |

### Slice 11 — `api/vitest.config.ts`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| SS1  | `line 1` | MUST | File exports default config but has no module-level JSDoc with `@packageDocumentation`. |
| 7.x  | `line 4` | MUST | Exported default symbol has no JSDoc block describing boundary and intent. |
| 1.9  | `line 7` | MUST | Uses `new URL(...)` without `DEVIATION(1.9)` justification. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| —   | —    | No deviations declared in this slice. |

### Slice 10 — `api/src/types/bcktrck-engine.d.ts`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| SS1  | `line 1` | MUST | Declaration file exports public API members but has no module-level JSDoc with `@packageDocumentation`. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| —   | —    | No deviations declared in this slice. |

### Slice 8 — `api/src/server.ts`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| SS1  | `line 1` | MUST | File exports public symbols but has no module-level JSDoc with `@packageDocumentation`. |
| 3.x  | `line 146` | MUST | `initialHeaders` uses mutable `Record<string, string>` rather than readonly record typing. |
| 6.3  | `line 187` | MUST | `Option` fallback emits `null` (`getOrElse((): string | null => null)`), propagating nullability in core flow. |
| LOC  | `line 302` | SHOULD | File length is 302 LOC, above the 300 LOC warning threshold in `loc` focus. |
| 1.9  | `line 38` | NOTE | `DEVIATION(1.9)` documents adapter-scoped `new Map(...)`. |
| 2.4  | `line 129` | NOTE | `DEVIATION(2.4)` documents mutable in-memory rate-limit state update. |
| 8.1  | `line 49` | NOTE | `DEVIATION(8.1)` documents explicit 413 problem response not represented in canonical `ApiError` taxonomy. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref            | Line      | Justification |
|----------------|-----------|---------------|
| DEVIATION(1.9) | `38,58-60,66-68,137-139,192-194,205-207,217-219` | Adapter boundary requires `new Map`, `new URL`, `new Request`, and `new Date` constructs. |
| DEVIATION(2.4) | `129-131` | In-memory adapter rate limiter requires mutable per-request state update. |
| DEVIATION(8.1) | `49-52`   | API boundary requires HTTP 413 payload-too-large response not represented in standard taxonomy. |

### Slice 9 — `api/src/test-support/engineShim.ts`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| SS1  | `line 1` | MUST | File has public exports but no module-level JSDoc with `@packageDocumentation`. |
| 7.x  | `line 39` | MUST | Exported symbol `defaultRenderConfig` has no JSDoc block. |
| 7.x  | `line 41` | MUST | Exported symbol `compile` has no JSDoc block. |
| 7.x  | `line 51` | MUST | Exported symbol `listSubtreesFromSource` has no JSDoc block. |
| 7.x  | `line 57` | MUST | Exported symbol `getStylePack` has no JSDoc block. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| —   | —    | No deviations declared in this slice. |

### Slice 4 — `api/src/routes/stylePack.ts`

**Status:** ✅ Fixed

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| SS1  | `line 1` | MUST | File exports a public symbol but has no module-level JSDoc with `@packageDocumentation`. |
| 8.2  | `line 17` | NOTE | `DEVIATION(8.2)` documents compatibility adapter for `fromZodError` with Zod v4 `issues`. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref            | Line   | Justification |
|----------------|--------|---------------|
| DEVIATION(8.2) | `17`   | Compatibility bridge between boundary Zod error shape and Zod v4 issue shape. |

### Slice 5 — `api/src/routes/subtrees.test.ts`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| 1.9  | `line 15` | NOTE | `new Request(...)` is covered by `DEVIATION(1.9)` at lines 13-14 for handler-boundary testing. |

#### Checklist

**Types and ADTs (§1)**
- [x] 1.1 — Sum types modelled as tagged discriminated union with literal discriminant
- [x] 1.2 — Exhaustive `switch` ends in `default: return absurd(x)`
- [x] 1.3 — Nominal distinctions via branded types; only smart constructors (`mk*`, `from*`, `as*`) cast with `as`
- [x] 1.4 — No bare `interface` (or `// DEVIATION(1.4): <reason>` present)
- [x] 1.5 — No `any`; `unknown` used at I/O boundaries, narrowed in scope
- [x] 1.6 — No `!`; no `as` outside smart constructor bodies
- [x] 1.8 — No `enum`; use string literal unions or `as const`
- [x] 1.9 — No `class` · `this` · `new` · `instanceof` · `namespace`
- [x] 1.11 — Prelude ADT discriminants accessed via exported guards only (`isOk`, `isSome`)
- [x] 1.12 — Discriminant convention: `_tag` for prelude ADTs · `kind` for domain ADTs

**Immutability (§2–§3)**
- [x] 2.1 — `const` for every binding; no `let` / `var`
- [x] 2.2 — `ReadonlyArray<T>` everywhere; no mutable arrays
- [x] 2.3 — No mutating methods (`push`, `pop`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`)
- [x] 2.4 — No property assignment or `delete` after construction
- [x] 2.5 — `as const` for literal narrowing and config tables
- [x] 3.x — `readonly` on every record field

**Control flow (§4)**
- [x] 4.1 — Every sum-type `switch` is exhaustive; `default: return absurd(x)`
- [x] 4.5 — No truthiness checks on non-booleans (`if (str)`, `if (value)`)
- [x] No `for` · `while` · `do..while`; use `map`, `filter`, `reduce`, `pipe`, or traversal combinators

**Pipelines and effects (§5–§6)**
- [x] 5.1 — Pipelines via `pipe` from `@tsfpp/prelude`
- [x] 6.2 — `throw` only at adapter boundaries; core uses `Result<T, E>`
- [x] 6.3 — No `null`/`undefined` propagation; use `Option<A>`
- [x] 6.6 — `Promise.allSettled` over `Promise.all` when partial failure is meaningful

**Annotations (§7)**
- [x] 7.x — JSDoc on every exported symbol (`@param`, `@returns`; `@law` on combinators)

**Boundary and imports (§8–§9)**
- [x] 8.4 — Parse, don't validate: `unknown` converted to domain types at the boundary
- [x] 9.x — No `import from 'ramda'`; use `@tsfpp/prelude`

**Size limits (§11)**
- [x] 11.1 — One type / one responsibility per file
- [x] 11.2 — File ≤ 400 LOC (800 absolute max with deviation)
- [x] Function body ≤ 40 lines · cyclomatic complexity ≤ 10 · nesting ≤ 4 · arity ≤ 3

#### Deviation register

| Ref            | Line   | Justification |
|----------------|--------|---------------|
| DEVIATION(1.9) | `13-14` | Fetch `Request` instantiation is required in adapter-boundary handler tests. |
