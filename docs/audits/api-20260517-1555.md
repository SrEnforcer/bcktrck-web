# TSF++ Audit — api

**Target:** api
**Focus:** security
**Standard:** @tsfpp/standard v1.0.0
**Date:** 2026-05-17 15:55
**Status:** ✅ Resolved

---

## Summary

All identified MUST security violations were remediated slice-by-slice:

1. API rule 7.1: Rate limiting control added to public API request path with `RateLimit-*` headers.
2. API rule 7.2: Payload size constraints and HTTP timeout budgets added in the Node adapter.
3. API rule 3.3: Request schemas now reject unknown fields in all affected route handlers.

| Category    | Violations | Deviations | Passed |
|-------------|-----------|------------|--------|
| Types       | 0         | 0          | 9      |
| Purity      | 0         | 0          | 9      |
| Boundary    | 0         | 2          | 9      |
| Annotations | 0         | 0          | 9      |
| Complexity  | 0         | 0          | 9      |

---

## Slices

| # | Path | Status |
|---|------|--------|
| 1 | `api/package.json` | ✅ |
| 2 | `api/src/server.ts` | ✅ |
| 3 | `api/src/routes/compile.ts` | ✅ |
| 4 | `api/src/routes/stylePack.ts` | ✅ |
| 5 | `api/src/routes/subtrees.ts` | ✅ |
| 6 | `api/src/types/bcktrck-engine.d.ts` | ✅ |
| 7 | `api/eslint.config.js` | ✅ |
| 8 | `api/tsconfig.json` | ✅ |
| 9 | `api/tsconfig.dev.json` | ✅ |

---

<!-- Slices are appended below as the audit progresses -->

### Slice 1 — `api/package.json`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | CLEAN | No hardcoded secrets found; dependencies are declared from package registries/workspace link with lockfile-managed resolution. |

#### Checklist

- [x] SEC 4 (Axiom) — Secrets are not present in source-controlled config
- [x] API 7.5 — No secrets in source, logs, or payload definitions
- [x] SEC dependency hygiene scope — No insecure install scripts or ad-hoc fetch/install patterns in package metadata

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 6 — `api/src/types/bcktrck-engine.d.ts`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | CLEAN | Type declaration module contains no runtime boundary, secret, auth, or error-surface code. |

#### Checklist

- [x] SEC 2.x — No runtime input boundary in this slice
- [x] API 4.3 — No runtime error payload composition in this slice
- [x] API 7.5 — No secrets in declarations

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 7 — `api/eslint.config.js`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | CLEAN | Lint config delegates to shared TSF++ preset; no security anti-patterns in local override. |

#### Checklist

- [x] SEC dependency hygiene scope — No unsafe custom lint bypasses in this slice
- [x] API 7.5 — No secrets in configuration

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 8 — `api/tsconfig.json`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | CLEAN | Compiler configuration shows no security-rule violation in boundary handling, auth, or secrets management. |

#### Checklist

- [x] SEC dependency/build hygiene scope — No insecure transpile target overrides found
- [x] API 7.5 — No secrets in configuration

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 9 — `api/tsconfig.dev.json`

**Status:** ✅ Clean

#### Findings

| Rule | Location | Severity | Finding |
|------|----------|----------|---------|
| — | — | CLEAN | Dev path mapping introduces no direct security boundary or secret exposure in this slice. |

#### Checklist

- [x] SEC dependency/build hygiene scope — No insecure local compiler overrides found
- [x] API 7.5 — No secrets in configuration

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 5 — `api/src/routes/subtrees.ts`

**Status:** ✅ Fixed

#### Resolved findings

| Rule | Prior location | Severity | Resolution |
|------|----------------|----------|------------|
| 3.3 (API) | `line 13` | MUST | Applied `.strict()` to `SubtreesRequestSchema` so unknown fields are rejected at boundary parse. |

#### Checklist

- [x] SEC 2.1 — Inbound body is parsed and validated at boundary
- [x] SEC 2.2 — Allow-list schema is used for accepted fields
- [x] API 3.3 — Unknown inbound fields are explicitly rejected
- [x] API 4.3 — Handler maps errors via boundary helpers without stack disclosure
- [x] API 7.5 — No secrets in request/response handling

#### Verification

- [x] `pnpm --filter @bcktrck/api typecheck`
- [x] `pnpm --filter @bcktrck/api lint -- src/routes/subtrees.ts`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 4 — `api/src/routes/stylePack.ts`

**Status:** ✅ Fixed

#### Resolved findings

| Rule | Prior location | Severity | Resolution |
|------|----------------|----------|------------|
| 3.3 (API) | `line 12` | MUST | Applied `.strict()` to `StylePackRequestSchema` so unknown fields are rejected at boundary parse. |

#### Checklist

- [x] SEC 2.1 — Inbound body is parsed and validated at boundary
- [x] SEC 2.2 — Allow-list schema is used for accepted fields
- [x] API 3.3 — Unknown inbound fields are explicitly rejected
- [x] API 4.3 — Handler maps errors via boundary helpers without stack disclosure
- [x] API 7.5 — No secrets in request/response handling

#### Verification

- [x] `pnpm --filter @bcktrck/api typecheck`
- [x] `pnpm --filter @bcktrck/api lint -- src/routes/stylePack.ts`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 3 — `api/src/routes/compile.ts`

**Status:** ✅ Fixed

#### Resolved findings

| Rule | Prior location | Severity | Resolution |
|------|----------------|----------|------------|
| 3.3 (API) | `line 13` | MUST | Applied `.strict()` to `CompileRequestSchema` so unknown fields are rejected at boundary parse. |

#### Checklist

- [x] SEC 2.1 — Inbound body is parsed and validated at boundary
- [x] SEC 2.2 — Allow-list schema is used for accepted fields
- [x] API 3.3 — Unknown inbound fields are explicitly rejected
- [x] API 4.3 — No internal diagnostic data returned in handler response mapping
- [x] API 7.5 — No secrets in request/response handling

#### Verification

- [x] `pnpm --filter @bcktrck/api typecheck`
- [x] `pnpm --filter @bcktrck/api lint -- src/routes/compile.ts`

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| None | — | — |

### Slice 2 — `api/src/server.ts`

**Status:** ✅ Fixed

#### Resolved findings

| Rule | Prior location | Severity | Resolution |
|------|----------------|----------|------------|
| 7.1 (API) | `line 68`, `line 72`, `line 76`, `line 80` | MUST | Added adapter-level rate-limit gate with in-memory window tracking and `RateLimit-*` headers on responses. |
| 7.2 (API) | `line 41`, `line 43` | MUST | Added `Content-Length` validation, explicit body-size cap (`MAX_BODY_BYTES`), post-read byte cap enforcement, and HTTP timeout budgets via `http.createServer` options. |

#### Checklist

- [x] SEC 2.1 — Boundary crossing exists and is explicit in adapter code
- [x] API 7.1 — Rate limiting enforced on all public endpoints
- [x] API 7.2 — Payload size limits and request timeout budgets enforced
- [x] API 7.5 — No secrets in source or response writes
- [x] API 4.3 — No stack traces/SQL/internal hosts exposed in response messages here

#### Verification

- [x] `pnpm --filter @bcktrck/api typecheck`
- [x] `pnpm --filter @bcktrck/api lint`
- [x] `pnpm --filter @bcktrck/api test` (project currently prints `No tests yet`)

#### Deviation register

| Ref | Line | Justification |
|-----|------|---------------|
| DEVIATION(1.9) | `api/src/server.ts` | Node adapter requires `new URL`/`new Request`/`new Map` bridge constructs and in-memory adapter state.
| DEVIATION(2.4) | `api/src/server.ts` | Adapter-scoped in-memory rate limiter requires state updates between requests (`Map#set`). |
